//  String extractCategoryName(String rawCategory, String langCode) {
// final regex = RegExp(r'\[' + langCode + r'=(.*?)\]');
// final match = regex.firstMatch(rawCategory);
// return match != null ? match.group(1)! : rawCategory;
// }
